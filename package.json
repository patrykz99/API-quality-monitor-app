{
  "name": "api-quality-monitor-app",
  "version": "1.0.0",
  "description": "API Quality Monitor is an app that lets you: add API endpoints, store them in a database, run endpoint tests, measure response times, check status codes, save test histories, and view results",
  "main": "index.js",
  "type": "module",
  "directories": {
    "test": "tests",
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.1",
    "tsx": "^4.20.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
